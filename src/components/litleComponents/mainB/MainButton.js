import React from 'react';
import './MainButton.css'

const mainButton = () => {
    return (
        <button className='main-button'> 
            <span>Apply Now</span>
        </button>
    );
};

export default mainButton;